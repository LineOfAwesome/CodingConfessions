define( function ( require ) {
	'use strict';

	var App = require( 'App' );

	App.module( 'Admin.Views', function ( Views ) {

		Views.PageLayout = require( 'apps/admin/views/AdminPageLayoutView' );
	} );

} );
